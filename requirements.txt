# HalluciGuard — backend dependencies
# ──────────────────────────────────────────────────────────────
# NOTE: gradio, fastapi, starlette, jinja2, uvicorn, pydantic
# are managed by the HF Gradio Space SDK (sdk_version in README.md).
# Do NOT pin them here — it causes version conflicts.
# ──────────────────────────────────────────────────────────────
pydantic-settings>=2.0
langgraph>=0.1.0
langchain-core>=0.2.0
httpx>=0.24.0
torch
transformers
sentence-transformers
datasets
scikit-learn
rank-bm25
faiss-cpu
aiosqlite
networkx
numpy<2.0
beautifulsoup4
lxml
pyyaml
python-dotenv
