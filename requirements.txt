# HalluciGuard — backend dependencies
# ──────────────────────────────────────────────────────────────
# Pinned compatibility locks for HF Gradio Space (sdk_version: 4.44.0)
# - starlette<0.38.0: fixes Jinja2 TemplateResponse "unhashable type: dict"
# - huggingface_hub<1.0.0: fixes "cannot import name HfFolder"
# ──────────────────────────────────────────────────────────────
starlette<0.38.0
fastapi<0.115.0
huggingface_hub<1.0.0
pydantic-settings>=2.0
langgraph>=0.1.0
langchain-core>=0.2.0
httpx>=0.24.0
torch
transformers
sentence-transformers
datasets
scikit-learn
rank-bm25
faiss-cpu
aiosqlite
networkx
numpy<2.0
beautifulsoup4
lxml
pyyaml
python-dotenv
# Claim decomposition (POS/dependency-aware atomic-claim extraction).
# The decomposer degrades to a regex fallback if these are unavailable.
spacy>=3.8.0
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
