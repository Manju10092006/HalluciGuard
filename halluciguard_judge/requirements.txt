# halluciguard_judge/requirements.txt
# New standalone detector dependencies
# (Most are already in the project root requirements.txt)

# Core ML
torch>=2.0.0
transformers>=4.40.0
sentencepiece>=0.2.0          # required by DeBERTa tokenizer

# Optional: faster tokenization
tokenizers>=0.19.0

# Training
scikit-learn>=1.3.0
scipy>=1.11.0                  # for calibration (minimize_scalar)
numpy>=1.24.0

# NLP utilities
nltk>=3.8.0                    # sentence tokenization (punkt)

# HTTP (LLM judge)
httpx>=0.27.0

# Data validation
pydantic>=2.0.0

# Optional: pydantic settings (for env var loading)
pydantic-settings>=2.0.0

# Evaluation
# matplotlib>=3.7.0            # for calibration curves (optional)
